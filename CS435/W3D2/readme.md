## Question 3. Given two Heaps H1 and H2 of the same height, give an efficient algorithm to “fuse together” to make one Heap.

```
1. Make all node to 1 node
2. Do bottom up build
```

```
(a)	What is the best case?  -> same nodes
(b)	What is the worst case? -> diffrent hieght and nodes
(c)	What is the worst case time complexity? -> O(n+m)
```